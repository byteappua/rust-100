# Day 10: 常用集合

## 1. Vector (Vec<T>)

```rust
let v = vec![1, 2, 3];
```

## 2. String

```rust
let s = String::from("hello");
```

## 3. HashMap

```rust
use std::collections::HashMap;
let mut map = HashMap::new();
map.insert("key", "value");
```
