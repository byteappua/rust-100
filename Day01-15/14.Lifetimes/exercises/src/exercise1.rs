fn main() {
    let r;
    {
        let x = 5;
        // Error: `x` does not live long enough
        // r = &x;

        // TODO: Fix this code so we can print `r`.
        // You can move `x` or `r` or change how r is assigned.
        // For the exercise, let's assume we want to print value 5.
    }
    // println!("r: {}", r);
}
