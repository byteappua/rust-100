fn main() {
    // TODO: Use `#[cfg(...)]` to define a function `platform_message`
    // that returns "Linux" if target_os is linux, and "Other" otherwise.

    // println!("Running on: {}", platform_message());
}
