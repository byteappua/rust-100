fn main() {
    let mut count = 0;

    // TODO: Define a closure `inc` that increments `count` by 1 and returns the new value.
    // Hint: Since `count` is modified, the closure must be mutable.
    // let mut inc = ...

    // println!("Count: {}", inc());
    // println!("Count: {}", inc());
    // println!("Count: {}", inc());
}
